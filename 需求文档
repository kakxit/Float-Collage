产品需求文档 (PRD)：沉浸式自由拼图工具 (PC端)
1. 产品概述
1.1 产品简介
本产品是一款轻量级、高自由度的PC端图片拼接与排版工具。与市面上传统的固定框架拼图软件不同，它主打“沉浸式”与“无边界”的操作体验，通过半透明界面、窗口即画布的交互方式，以及强大的自定义参考线系统，帮助用户更直观、高效地完成图片拼接与排版。

1.2 目标用户与使用场景

目标用户： 设计师、自媒体创作者、学生、研究人员以及需要频繁处理截图与长图拼接的用户。

核心场景： 需要将多张网页截图拼接成长图，且需要参考桌面其他内容；需要精准对齐图片元素；需要快速排除图片边缘的无效信息（如浏览器边框、水印）。

2. 核心功能需求 (Functional Requirements)
2.1 现代图形用户界面 (GUI)
需求描述： 提供极简、现代的PC端软件界面。

交互说明：

隐藏式工具栏： 默认状态下工具栏收起或以悬浮球/半透明面板形式存在，最大化保留用户的拼接视野。

快捷键支持： 支持常用的快捷键操作（如 Ctrl+C/V 导入图片，Ctrl+Z 撤销，Space+鼠标拖拽 移动画布）。

2.2 半透明拼接工作台
需求描述： 软件的背景画布支持半透明显示，用户可以透过软件看到下方的电脑桌面或其他软件界面。

功能细节：

透明度调节： 提供一个滑块或快捷键（如 Alt + 滚轮），支持画布背景透明度在 0%（全透明）到 100%（纯色/不透明）之间无缝切换。

始终顶层： 软件始终保持在所有窗口的最上层。

2.3 窗口级自由比例调节 (核心亮点)
需求描述： 打破常规的“在软件内部输入长宽数值”改变画布大小的逻辑，将软件系统窗口的物理大小与拼接画布大小强绑定。

交互说明：

常规模式（预设比例）： 提供标准的 1:1, 16:9, 4:3, 9:16 等固定比例选项。选择后，软件窗口自动改变为对应比例。

自由模式（窗口缩放）： 用户直接将鼠标移动到 Windows 软件窗口的边缘或四角，出现系统自带的双向箭头后，拖动拉伸窗口。窗口变大/变小，内部的拼接画布计算区域同步无损拉伸/缩放。

自动贴边与吸附： 拖动窗口调节大小时，画布内的图片元素跟随画布边缘自适应缩放或保持原比例贴边。

2.4 高级参考线系统
需求描述： 提供高自由度的参考线生成与控制功能，辅助精准对齐。

功能细节：

自动生成： 用户输入横向（X轴）和竖向（Y轴）的参考线数量（例如：横向生成3条，竖向生成2条），系统在当前画布内自动等距生成网格参考线。

动态联动拖拽： 支持拖动第一根或最后一根参考线（即边缘参考线）。 当用户拖动边缘参考线改变网格的总范围时，中间的其他参考线会自动按比例重新计算并等距分布（类似手风琴的拉伸效果）。

磁吸效应： 图片边缘或中心点靠近参考线时，自动产生像素级吸附感（Snap to Grid）。

2.5 极简拖拽导入 (Drag & Drop Import)
需求描述： 允许用户脱离传统的“文件 -> 打开”弹窗，通过系统原生的拖拽动作，将外部图片直接加载到拼接工作台中。

多文件支持： 支持从 Windows 资源管理器一次性拖拽单张图片、多张图片甚至整个文件夹进入软件窗口。多图拖入时，系统自动在画布空白区域将其错开排列，避免完全重叠。

视觉反馈： 当用户拖拽文件进入软件窗口范围内时，画布边缘出现高亮边框或半透明遮罩，提示“松开鼠标以导入图片”。

容错处理： 若用户拖入了不支持的文件格式（如 .exe 或 .doc），鼠标指针需变为禁用图标，并在松开后给予轻量级的“不支持该格式”的文字提示。

2.6 智能边缘吸附与对齐系统 (Smart Snapping)
需求描述： 在原有的“参考线吸附”基础上，新增图像实体之间的磁吸与对齐机制，帮助用户快速实现像素级的无缝拼接。

轮廓边缘磁吸： 当用户拖动图片 A 靠近图片 B 的任意边缘（距离小于设定的阈值，如 5px）时，图片 A 会自动吸附并紧紧贴合图片 B 的边缘，实现无缝缝合。

智能临时参考线： 在拖拽移动图片时，若当前图片的中心点或边缘与画布内其他图片的中心点或边缘对齐，系统需短暂显现一条高亮的临时对齐线（如绿色虚线），并伴随轻微的吸附停顿感。

临时屏蔽吸附： 提供快捷键（如按住 Alt 键拖动），允许用户在拖拽时暂时关闭所有的磁吸和对齐效果，以便进行不规则的、像素级的自由微调。

2.7 图层管理

 增加简单的图层面板，支持图片的上下层级调整。

3. 非功能性需求 (Non-Functional Requirements)
性能要求：

兼容性： 适配 Windows 10 及 Windows 11 操作系统。

导出格式： 支持导出为 PNG（保留透明通道）, JPG, WebP 等主流格式。